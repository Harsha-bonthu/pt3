# Example environment variables for PT-3 demo
# Copy this to `.env` and fill values as needed (do NOT commit secrets).

# Secret used to sign JWTs
SECRET_KEY=change-me-to-a-long-random-string

# Database URL: defaults to sqlite file in project root
# Examples:
# SQLite (default): sqlite:///./pt3.db
# Postgres: postgresql://user:password@localhost:5432/pt3db
# MySQL: mysql+pymysql://user:password@localhost:3306/pt3db
DATABASE_URL=sqlite:///./pt3.db

# Token lifetimes
ACCESS_TOKEN_EXPIRE_MINUTES=1440
REFRESH_TOKEN_EXPIRE_DAYS=7

# Optional: run Playwright tests with a single worker in CI for stability
# PLAYWRIGHT_WORKERS=1
